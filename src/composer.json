{
    "name": "pena/shipping-cost",
    "type": "project",
    "autoload": {
        "psr-4": {
            "App\\": ""
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "scripts": {
        "test": "vendor/bin/phpunit",
        "code-style": "vendor/bin/phpcs --standard=PSR12 --encoding=utf-8 -p Components config Deliveries Entities tests",
        "code-style-fix": "vendor/bin/phpcbf --standard=PSR12 --encoding=utf-8 -p Components config Deliveries Entities tests"
    }
}
